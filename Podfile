# Uncomment the next line to define a global platform for your project
platform :macos, '10.14'

target 'BBLoginWindow' do
  # use_frameworks!
end

target 'BootBuddy' do
  use_frameworks!
  pod 'Sparkle'
  pod 'AppCenter'
  pod 'LetsMove'
end
